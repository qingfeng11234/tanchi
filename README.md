# 贪吃蛇小游戏

一个纯前端实现的贪吃蛇小游戏，直接打开 `index.html` 即可游玩。

## 玩法

- 点击“开始 / 重新开始”按钮启动游戏。
- 使用方向键或 `W/A/S/D` 控制蛇移动。
- 吃到红色食物会加分并变长。
- 撞墙或撞到自己则游戏结束。
- 最高分会保存在浏览器 `localStorage` 中。

## 本地运行

直接双击 `index.html`，或在项目目录运行一个静态服务：

```bash
python3 -m http.server 8000
```

然后访问 `http://localhost:8000`。
